meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: true
run:
  persistenceMount: /data
  image: iksem
  command: node server.js
  args: null
  containerPort: 3000
