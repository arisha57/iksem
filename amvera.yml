meta:
  version: 1.0
  toolchain:
    name: docker
    version: latest

app:
  name: iksem
  region: Moscow-0

build:
  docker:
    dockerfile: Dockerfile

run:
  image: iksem
  command: node server.js
  persistenceMount: /data
  containerPort: 3000
